{
  "name": "foundry-governance",
  "version": "1.0.0",
  "private": true,
  "description": "Microsoft Foundry enterprise governance presentation",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "marp presentation/slides.md --output _site/index.html --no-html",
    "test": "node --test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/frkim/foundry-governance.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@marp-team/marp-cli": "4.5.1"
  },
  "overrides": {
    "puppeteer-core": {
      "@puppeteer/browsers": "3.2.2"
    },
    "speech-rule-engine": {
      "@xmldom/xmldom": "0.9.12"
    }
  }
}
